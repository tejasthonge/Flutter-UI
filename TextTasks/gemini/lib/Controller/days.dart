import 'package:flutter/material.dart';

class Yesterday with ChangeNotifier {
  String yesterdayiscretion;
  Yesterday({required this.yesterdayiscretion});
}

class Today with ChangeNotifier {
  String todayDiscretion;
  Today({required this.todayDiscretion});
}

class Tomorrow with ChangeNotifier {
  String tomorrowDiscretion;
  Tomorrow({required this.tomorrowDiscretion});
}

